<Window x:Name="DS4_Updater" x:Class="DS4Updater.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="DS4 Updater" Height="Auto" SizeToContent="WidthAndHeight" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">

    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressValue="0" ProgressState="Normal"/>
    </Window.TaskbarItemInfo>
    <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="label1" Grid.Row="0" Grid.Column="0" Text="Fetching Download" VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="700" />
            <Label x:Name="label2" Grid.Row="0" Grid.Column="1" Content="time left" HorizontalAlignment="Right" Opacity="0" HorizontalContentAlignment="Right" />

            <ProgressBar x:Name="UpdaterBar" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Height="20" Margin="0,6,0,6" Maximum="106" />

            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button x:Name="btnRetry" Margin="0,0,6,0" Click="BtnRetry_Click" Visibility="Collapsed">
                    <TextBlock Text="Retry Download" TextWrapping="NoWrap" TextAlignment="Center" />
                </Button>
                <Button x:Name="btnChangelog" Margin="0,0,6,0" Click="BtnChangelog_Click">
                    <TextBlock Text="View Changelog" TextWrapping="NoWrap" TextAlignment="Center" />
                </Button>
                <Button x:Name="btnOpenDS4" Margin="0,0,0,0" IsEnabled="False" Click="BtnOpenDS4_Click" Background="#FFF0F0F0">
                    <TextBlock Text="Run DS4Windows" TextWrapping="NoWrap" TextAlignment="Center" />
                </Button>
            </StackPanel>
        </Grid>
</Window>
